"use strict";

//Area of circle calculator

var pi = 3.14;
var radius = 12; //value can be changed as needed
var area = (pi * (radius ** 2));

console.log("The area of a circle with a radius of " + radius + " is " + area);